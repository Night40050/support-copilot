version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      MOCK_LLM: "true"
      SUPABASE_URL: ${SUPABASE_URL}
      SUPABASE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}
    env_file:
      - .env
